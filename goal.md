# HEIC Compressor 持续优化目标

## 项目定位

HEIC Compressor 是一个面向 Android 10+ 的本地图片转 HEIC / 压缩工具，目标是在不引入不必要复杂度的前提下，持续提升真实设备上的压缩质量、处理效率、兼容性和易用性。

项目应优先服务以下核心场景：

- 将常见图片格式（JPG / PNG / WebP / BMP 等）稳定转换为 HEIC。
- 批量压缩图片并尽量减小文件体积。
- 在可控风险下保留必要元数据。
- 让用户能清楚预估压缩效果，并方便保存或分享结果。

## 长期目标

持续把项目优化到一个轻量、可靠、好用的 Android HEIC 压缩工具：

1. **稳定可靠**
   - 转换流程在主流 Android 设备上尽量稳定。
   - 对无 HEIC / HEIF 编码能力、异常图片、超大图片、权限失败、存储失败等情况给出明确提示。
   - 批量处理时单个文件失败不应导致整批任务不可恢复。

2. **压缩效果可控**
   - 持续优化质量参数、输出体积、视觉效果之间的平衡。
   - 优先避免“压缩后反而更大”的结果误导用户。
   - 保持预览结果与实际输出尽量一致。

3. **用户体验简洁**
   - 保持导入、预览、转换、保存、分享路径清晰。
   - 批量任务应提供明确进度、结果汇总和失败原因。
   - 设置项应少而有用，避免把技术细节直接暴露成复杂配置。

4. **设备与系统兼容**
   - 以 Android 10 / API 29+ 为基础兼容目标。
   - 针对不同厂商 HEIC 编码器差异做防御式处理。
   - 对权限、MediaStore、分享 Intent、系统图片选择器等 Android 平台行为保持兼容。

5. **可维护与可验证**
   - 代码结构保持清晰，转换、存储、元数据、UI 状态管理边界明确。
   - 每轮功能修改尽量配套最小可行验证。
   - 避免引入与图片压缩目标无关的大型依赖或后台服务。

## 优化优先级

后续持续优化时，默认按以下顺序判断价值：

1. 修复会导致崩溃、数据丢失、保存失败、批量中断的缺陷。
2. 提升主流程成功率、错误提示和恢复能力。
3. 改善压缩体积、质量预览、结果对比和批量效率。
4. 优化界面易用性、状态反馈和设置组织。
5. 清理代码结构、测试覆盖、构建配置和文档。

## 硬边界

- 不得默认覆盖或删除用户原图；任何替换原图行为必须明确、可理解、可取消。
- 不得静默丢弃用户显式要求保留的关键元数据。
- 不得在没有明确必要的情况下上传图片或依赖云端服务；默认应保持本地处理。
- 不得为了追求压缩率牺牲基本可读性、可恢复性和用户信任。
- 不得把与 HEIC 压缩无关的功能扩展成主要方向。

## 每轮迭代要求

每次推进项目时，应尽量完成一个可验证的小闭环：

- 明确本轮解决的问题或改进点。
- 修改后至少执行与改动范围匹配的构建、静态检查或手工验证。
- 更新必要文档或版本说明。
- 使用清晰的 git commit message 提交变更。

除非用户另行指定，后续优化应围绕本文件目标自主选择最有价值的下一步。
